/**
 * @deprecated Use App.ts via `npm run dev` (tsx) or compiled `npm start`.
 * This stub exists only to fail fast if something still points at App.js.
 */
console.error(
  '[ApiAdmin] Server/App.js is deprecated and removed from the runtime path.\n' +
    '  Development: npm run dev   (tsx watch App.ts)\n' +
    '  Production:  npm start     (node dist/Server/App.js)'
);
process.exit(1);
